> 已吸收至：[[07_工程与架构/0703_工程实践与质量保障/070301_可观测性与AIOps/070301_核心知识点/结构化日志与LLM_RCA证据边界|结构化日志与LLM_RCA证据边界]]
---
title: Citrix Netscaler全自动巡检平台，再也不用点点点啦！又省了2小时摸鱼时间
author: 虚拟化时代君
date:
url: https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247496917&idx=1&sn=20cc72a39fc262b9b60e0a75ab1e7251&chksm=cf2a3cd6508b58b22e764091c40c69d8965a040dbd98cfab839856e0986679f2bce744d0cea0&mpshare=1&scene=24&srcid=12176PwuP8gOff2ET2J5ighS&sharer_shareinfo=2ac9ec00b8047685be52d321591e8e51&sharer_shareinfo_first=2ac9ec00b8047685be52d321591e8e51#rd
---

哈喽大家好，欢迎来到**虚拟化时代君（XNHCYL），收不到通知请将我点击星标！****“**   大家好，我是虚拟化时代君，一位潜心于互联网的技术宅男。这里每天为你分享各种你感兴趣的技术、教程、软件、资源、福利…（每天更新不间断，福利不见不散）

**引言**

最近在整合集团数据中心架构，做集群和上面业务的巡检，今天用Python整理下巡检Citrix Netscaler环境的一键式脚本。

第一章、采用技术

我们部署在一台内网Linux机器上，推荐Rocky，采用技术如下：

后端： Flask (Python)

前端： 原生 HTML + CSS + JavaScript

依赖库： requests、paramiko、python-docx

备注：纯免费分享，不做打赏之后提供源码等服务，自己可根据代码更改！

第二章、效果展示

下图所示为巡检界面效果展示图。

下图所示为巡检后效果图

第二章、部分代码

```
#!/usr/bin/env python3# -*- coding: utf-8 -*-import requestsimport paramikofrom datetime import datetimefrom flask import Flask, render_template, jsonify, request, send_fileimport urllib3import iourllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)app = Flask(__name__)class NetscalerInspector:    def __init__(self, host, username, password, use_nitro=True, port=443):        self.host = host        self.username = username        self.password = password        self.use_nitro = use_nitro        self.port = port        self.session = None        self.ssh_client = None            def connect_nitro(self):        try:            url = f"https://{self.host}:{self.port}/nitro/v1/config/login"            payload = {"login": {"username": self.username, "password": self.password}}            self.session = requests.Session()            self.session.verify = False            response = self.session.post(url, headers={'Content-Type': 'application/json'}, json=payload, timeout=10)            return response.status_code == 201        except:            return False        def connect_ssh(self):        try:            self.ssh_client = paramiko.SSHClient()            self.ssh_client.set_missing_host_key_policy(paramiko.AutoAddPolicy())            self.ssh_client.connect(self.host, username=self.username, password=self.password, timeout=10)            return True        except:            return False        def nitro_get(self, endpoint):        try:            url = f"https://{self.host}:{self.port}/nitro/v1/stat/{endpoint}"            response = self.session.get(url, timeout=10)            return response.json() if response.status_code == 200 else None        except:            return None        def nitro_config_get(self, endpoint):        try:            url = f"https://{self.host}:{self.port}/nitro/v1/config/{endpoint}"            response = self.session.get(url, timeout=10)            return response.json() if response.status_code == 200 else None        except:            return None        def ssh_exec(self, command):        try:            stdin, stdout, stderr = self.ssh_client.exec_command(command)            return stdout.read().decode('utf-8')        except:            return ""        def check_system(self):        if self.use_nitro:            data = self.nitro_get("ns")            if data:                ns = data.get('ns', {})                ns_data = ns if isinstance(ns, dict) else (ns[0] if isinstance(ns, list) and ns else {})                return {                    "status": "ok",                    "data": {                        "主机名": ns_data.get("hostname", "N/A"),                        "HA状态": ns_data.get("hacurmasterstate", "N/A"),                        "启动时间": ns_data.get("starttime", "N/A"),                        "CPU使用率": ns_data.get("cpuusage", 0),                        "内存使用率": ns_data.get("memusagepcnt", 0),                        "磁盘使用率": ns_data.get("disk0perusage", 0),                        "型号": ns_data.get("modelid", "N/A"),                        "Packet_CPU": ns_data.get("pktcpuusagepcnt", 0),                        "Mgmt_CPU": ns_data.get("mgmtcpuusagepcnt", 0)                    }                }        else:            output = self.ssh_exec("stat system")            if output:                return {"status": "ok", "data": {"raw": output}}        return {"status": "ok", "data": {"主机名": "N/A", "HA状态": "N/A", "启动时间": "N/A", "CPU使用率": 0, "内存使用率": 0, "磁盘使用率": 0, "型号": "N/A", "Packet_CPU": 0, "Mgmt_CPU": 0}}        def check_services(self):        services = []        if self.use_nitro:            data = self.nitro_get("service")            if data and 'service' in data:                for svc in data['service']:                    state = svc.get("state", "N/A")                    services.append({                        "服务名称": svc.get("name", "N/A"),                        "状态": "运行中" if state == "UP" else ("停止" if state == "DOWN" else state),                        "原始状态": state,                        "IP地址": svc.get("primaryipaddress", "N/A"),                        "端口": svc.get("primaryport", "N/A")                    })        else:            output = self.ssh_exec("stat service")            if output:                return {"status": "ok", "data": [{"raw": output}]}        return {"status": "ok", "data": services}        def check_vservers(self):        vservers = []        if self.use_nitro:            data = self.nitro_get("lbvserver")            if data and 'lbvserver' in data:                for vs in data['lbvserver']:                    state = vs.get("state", "N/A")                    vservers.append({                        "虚拟服务器名称": vs.get("name", "N/A"),                        "状态": "运行中" if state == "UP" else ("停止" if state == "DOWN" else state),                        "原始状态": state,                        "健康度": f"{vs.get('vslbhealth', 0)}%",                        "类型": vs.get("type", "N/A"),                        "IP地址": vs.get("primaryipaddress", "N/A"),                        "请求数": vs.get("totalrequests", 0)                    })        else:            output = self.ssh_exec("stat lb vserver")            if output:                return {"status": "ok", "data": [{"raw": output}]}        return {"status": "ok", "data": vservers}        def check_gateway_vservers(self):        gw_list = []        if self.use_nitro:            data = self.nitro_get("vpnvserver")            if data and 'vpnvserver' in data:                for gw in data['vpnvserver']:                    state = gw.get("state", "N/A")                    gw_list.append({                        "Gateway名称": gw.get("name", "N/A"),                        "状态": "运行中" if state == "UP" else ("停止" if state == "DOWN" else state),                        "原始状态": state,                        "当前用户数": gw.get("actusers", 0),                        "IP地址": gw.get("primaryipaddress", "N/A"),                        "端口": gw.get("primaryport", "N/A"),                        "总请求数": gw.get("totalrequests", 0)                    })        else:            output = self.ssh_exec("stat vpn vserver")            if output:                return {"status": "ok", "data": [{"raw": output}]}        return {"status": "ok", "data": gw_list}        def check_sta_status(self):        sta_list = []        if self.use_nitro:            data = self.nitro_config_get("vpnglobal_staserver_binding")            if data and 'vpnglobal_staserver_binding' in data:                for sta in data['vpnglobal_staserver_binding']:                    state = sta.get("stastate", "N/A")                    sta_list.append({                        "STA服务器地址": sta.get("staserver", "N/A"),                        "状态": "正常" if state == "UP" else ("异常" if state == "DOWN" else state),                        "原始状态": state                    })        else:            output = self.ssh_exec("show vpn staserver")            if output:                return {"status": "ok", "data": [{"raw": output}]}        return {"status": "ok", "data": sta_list}        def check_interfaces(self):        interfaces = []        if self.use_nitro:            data = self.nitro_get("Interface")            if data and 'Interface' in data:                for iface in data['Interface']:                    state = iface.get("curlinkstate", "N/A")                    interfaces.append({                        "接口ID": iface.get("id", "N/A"),                        "状态": "已连接" if state == "UP" else ("已断开" if state == "DOWN" else state),                        "原始状态": state,                        "发送速率": f"{iface.get('txbytesrate', 0)} B/s",                        "接收速率": f"{iface.get('rxbytesrate', 0)} B/s",                        "错误包数": iface.get("errdroppedrxpkts", 0)                    })        else:            output = self.ssh_exec("stat interface")            if output:                return {"status": "ok", "data": [{"raw": output}]}        return {"status": "ok", "data": interfaces}        def check_ssl_certs(self):        certs = []        server_cert_count = 0        if self.use_nitro:            data = self.nitro_config_get("sslcertkey")            if data and 'sslcertkey' in data:                for cert in data['sslcertkey']:                    days = cert.get("daystoexpiration", -1)                    linkcert = cert.get("linkcertkeyname", "")                    cert_type = "CA/中间证书" if linkcert else "服务器证书"                    if not linkcert:                        server_cert_count += 1                    if days == -1:                        exp_status, exp_class = "未知", "unknown"                    elif days <= 0:                        exp_status, exp_class = "已过期", "expired"                    elif days <= 30:                        exp_status, exp_class = "即将过期", "warning"                    elif days <= 90:                        exp_status, exp_class = "注意", "notice"                    else:                        exp_status, exp_class = "正常", "ok"                    certs.append({                        "证书名称": cert.get("certkey", "N/A"),                        "证书类型": cert_type,                        "剩余天数": days if days >= 0 else "N/A",                        "有效期状态": exp_status,                        "状态类": exp_class,                        "证书状态": cert.get("status", "N/A")                    })        else:            output = self.ssh_exec("show ssl certKey")            if output:                return {"status": "ok", "data": [{"raw": output}], "server_cert_count": 0}        return {"status": "ok", "data": certs, "server_cert_count": server_cert_count}        def check_dns_servers(self):        dns_list = []        if self.use_nitro:            data = self.nitro_config_get("dnsnameserver")            if data and 'dnsnameserver' in data:                for dns in data['dnsnameserver']:                    state = dns.get("state", "N/A")                    dns_list.append({                        "DNS服务器IP": dns.get("ip", "N/A"),                        "状态": "启用" if state == "ENABLED" else ("禁用" if state == "DISABLED" else state),                        "原始状态": state,                        "类型": dns.get("type", "N/A"),                        "端口": dns.get("port", 53)                    })        else:            output = self.ssh_exec("show dns nameServer")            if output:                return {"status": "ok", "data": [{"raw": output}]}        return {"status": "ok", "data": dns_list}        def check_license(self):        if self.use_nitro:            data = self.nitro_config_get("nslicense")            if data and 'nslicense' in data:                lic = data['nslicense']                lic_data = lic if isinstance(lic, dict) else (lic[0] if isinstance(lic, list) and lic else {})                return {                    "status": "ok",                    "data": {                        "设备型号": lic_data.get("modelid", "N/A"),                        "标准版许可": "是" if lic_data.get("isstandardlic") else "否",                        "企业版许可": "是" if lic_data.get("isenterpriselic") else "否",                        "铂金版许可": "是" if lic_data.get("isplatinumlic") else "否",                        "SSL_VPN用户数": lic_data.get("f_sslvpn_users", "N/A"),                        "授权带宽": lic_data.get("f_bandwidth", "N/A")                    }                }        else:            output = self.ssh_exec("show ns license")            if output:                return {"status": "ok", "data": {"raw": output}}        return {"status": "ok", "data": {}}        def check_ha_status(self):        ha_nodes = []        if self.use_nitro:            data = self.nitro_get("hanode")            if data and 'hanode' in data:                for node in data['hanode']:                    state = node.get("state", "N/A")                    ha_nodes.append({                        "节点ID": node.get("id", "N/A"),                        "节点状态": "主节点" if state == "Primary" else ("备节点" if state == "Secondary" else state),                        "原始状态": state,                        "IP地址": node.get("ipaddress", "N/A"),                        "同步状态": node.get("hasync", "N/A")                    })        else:            output = self.ssh_exec("stat ha node")            if output:                return {"status": "ok", "data": {"raw": output}}        return {"status": "ok", "data": {"nodes": ha_nodes}}        def run_full_inspection(self):        if self.use_nitro:            if not self.connect_nitro():                return {"error": "无法连接到Netscaler NITRO API，请检查IP、端口、用户名和密码"}        else:            if not self.connect_ssh():                return {"error": "无法通过SSH连接到Netscaler，请检查连接信息"}                result = {            "timestamp": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),            "host": self.host,            "system": self.check_system(),            "services": self.check_services(),            "vservers": self.check_vservers(),            "gateway_vservers": self.check_gateway_vservers(),            "sta_status": self.check_sta_status(),            "interfaces": self.check_interfaces(),            "ssl_certs": self.check_ssl_certs(),            "dns_servers": self.check_dns_servers(),            "license": self.check_license(),            "ha_status": self.check_ha_status()        }                try:            self.session.post(f"https://{self.host}:{self.port}/nitro/v1/config/logout", json={"logout": {}})        except:            pass                return result@app.route('/')def index():    return render_template('index.html')@app.route('/api/inspect', methods=['POST'])def inspect():    try:        data = request.get_json()        inspector = NetscalerInspector(            data.get('host', ''),            data.get('username', ''),            data.get('password', ''),            data.get('use_nitro', True),            data.get('port', 443)        )        return jsonify(inspector.run_full_inspection())    except Exception as e:        return jsonify({"error": str(e)}), 500@app.route('/api/export-word', methods=['POST'])def export_word():    try:        from docx import Document        from docx.shared import Pt        from docx.enum.text import WD_ALIGN_PARAGRAPH        from docx.oxml.ns import qn                data = request.get_json()        doc = Document()                style = doc.styles['Normal']        style.font.name = '微软雅黑'        style._element.rPr.rFonts.set(qn('w:eastAsia'), '微软雅黑')                title = doc.add_heading('Citrix NetScaler 巡检报告', 0)        title.alignment = WD_ALIGN_PARAGRAPH.CENTER                doc.add_paragraph(f"巡检时间: {data.get('timestamp', 'N/A')}")        doc.add_paragraph(f"目标设备: {data.get('host', 'N/A')}")        doc.add_paragraph("")                doc.add_heading('一、巡检总结', level=1)        sys_data = data.get('system', {}).get('data', {})        vservers = data.get('vservers', {}).get('data', [])        gw = data.get('gateway_vservers', {}).get('data', [])        ssl = data.get('ssl_certs', {}).get('data', [])                doc.add_paragraph(f"• 设备型号: {sys_data.get('型号', 'N/A')}")        doc.add_paragraph(f"• HA状态: {sys_data.get('HA状态', 'N/A')}")        doc.add_paragraph(f"• CPU使用率: {sys_data.get('CPU使用率', 0)}%")        doc.add_paragraph(f"• 内存使用率: {sys_data.get('内存使用率', 0)}%")                vs_up = len([v for v in vservers if v.get('原始状态') == 'UP']) if isinstance(vservers, list) else 0        doc.add_paragraph(f"• 负载均衡虚拟服务器: {vs_up}/{len(vservers) if isinstance(vservers, list) else 0} 运行中")                total_users = sum([g.get('当前用户数', 0) for g in gw]) if isinstance(gw, list) else 0        doc.add_paragraph(f"• Gateway当前用户数: {total_users}")                server_certs = [c for c in ssl if c.get('证书类型') == '服务器证书'] if isinstance(ssl, list) else []        doc.add_paragraph(f"• 服务器证书数: {len(server_certs)}")        expiring = [c for c in ssl if c.get('状态类') in ['warning', 'expired']] if isinstance(ssl, list) else []        if expiring:            doc.add_paragraph(f"• ⚠️ 即将过期/已过期证书: {len(expiring)}个")                def add_table_section(title, items, keys):            doc.add_heading(title, level=1)            if not items:                doc.add_paragraph("暂无数据")                return            if isinstance(items, list) and len(items) > 0:                if 'raw' in items[0]:                    doc.add_paragraph(items[0]['raw'])                else:                    table = doc.add_table(rows=1, cols=len(keys))                    table.style = 'Table Grid'                    for i, k in enumerate(keys):                        table.rows[0].cells[i].text = k                    for item in items:                        row = table.add_row()                        for i, k in enumerate(keys):                            row.cells[i].text = str(item.get(k, ''))                def add_dict_section(title, d):            doc.add_heading(title, level=1)            if not d:                doc.add_paragraph("暂无数据")                return            if 'raw' in d:                doc.add_paragraph(d['raw'])            else:                for k, v in d.items():                    if k not in ['nodes', 'raw']:                        doc.add_paragraph(f"{k}: {v}")                add_dict_section('二、系统状态', sys_data)                ha_data = data.get('ha_status', {}).get('data', {})        if ha_data.get('nodes'):            add_table_section('三、高可用状态', ha_data['nodes'], ['节点ID', '节点状态', 'IP地址', '同步状态'])        else:            doc.add_heading('三、高可用状态', level=1)            doc.add_paragraph("单机模式")                add_table_section('四、负载均衡虚拟服务器', vservers, ['虚拟服务器名称', '状态', '健康度', '类型', 'IP地址'])        add_table_section('五、Gateway虚拟服务器', gw, ['Gateway名称', '状态', '当前用户数', 'IP地址', '端口'])        add_table_section('六、服务状态', data.get('services', {}).get('data', []), ['服务名称', '状态', 'IP地址', '端口'])        add_table_section('七、网络接口', data.get('interfaces', {}).get('data', []), ['接口ID', '状态', '发送速率', '接收速率'])        add_table_section('八、SSL证书', ssl, ['证书名称', '证书类型', '有效期状态', '剩余天数'])        add_table_section('九、DNS服务器', data.get('dns_servers', {}).get('data', []), ['DNS服务器IP', '状态', '类型', '端口'])        add_dict_section('十、许可证信息', data.get('license', {}).get('data', {}))                buffer = io.BytesIO()        doc.save(buffer)        buffer.seek(0)                return send_file(buffer, as_attachment=True, download_name=f'NetScaler巡检报告_{datetime.now().strftime("%Y%m%d_%H%M%S")}.docx', mimetype='application/vnd.openxmlformats-officedocument.wordprocessingml.document')    except ImportError:        return jsonify({"error": "请先安装python-docx: pip install python-docx"}), 500    except Exception as e:        return jsonify({"error": str(e)}), 500if __name__ == '__main__':    app.run(host='0.0.0.0', port=5000, debug=False)
```

【以上内容均属虚拟化时代君整理，大家仅供参考！】

---

🌟这里是一个为你提供实用干货、深度分析和最新趋势的地方！无论你是对职业发展、健康生活、科技创新，还是人文艺术感兴趣，我们都能满足你的需求。

💡 每日更新，帮你提升职场竞争力

🔥 实用技巧，让生活更简单高效

💬 与志同道合的朋友们一起交流，分享灵感

👉 扫码关注，精彩内容马上送到你手中！你也可以与我们互动，告诉我们你最想看到的内容哦！🚀

**往期回顾**

**点击下方链接阅读精彩文章**

[Citrix Netscaler 又更新了、授权模式已完全变化，您的设备还授权正常吗？](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247496814&idx=1&sn=9ed08217f99e6bd13480f804fedfd5b0&scene=21#wechat_redirect)

[Citrix Netscaler又更新了？新版支持Nutanix AHV导入！](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247496794&idx=1&sn=23b5fba184361fff5a54b09b6360de50&scene=21#wechat_redirect)

[漏洞风险再次升级 | Citrix NetScaler(CVE-2025-5349 & 5777)已遭在野利用，紧急排查！](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247496347&idx=1&sn=94a51b868cb9d983303a839bb923ca68&scene=21#wechat_redirect)

[全球再现9.2分高危漏洞，NetScaler ADC / Gateway CVE-2025-6543 已复现可使核心服务瘫痪](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247496116&idx=1&sn=379a3ce928cfc778e2b353b21143f7aa&scene=21#wechat_redirect)

[席卷全球9.3分Citrix NetScaler 高危漏洞CVE-2025-5349 / 5777 曝光，别等被黑了才升级！](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247495764&idx=1&sn=b76d2f564642be612d68f5a1ff34c148&scene=21#wechat_redirect)

[VMware紧急修复！VMSA-2025-0010：又现CVE-2025-41225/41226高危漏洞，你的虚拟化还安全吗？](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494927&idx=1&sn=c22ba5e2028c510f88abadc7071d585e&scene=21#wechat_redirect)

[Citrix云桌面又更新了？CVAD 2503测试版本发布，功能遥遥领先！](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494499&idx=1&sn=bd42733639359cc3523844018108399c&scene=21#wechat_redirect)

[技术干货 | VMware vCenter HA高可用配置实战指南（内含完整演示）主备切换只需秒级？](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494913&idx=1&sn=223d8b2cfc472800afd7b323f3f6b09d&scene=21#wechat_redirect)

[想省钱、想升职、想搞定老板？不想删库跑路那就先折腾运维神器-超聚变模拟器](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494837&idx=1&sn=317a82f9c7d171f30b147990678175c0&scene=21#wechat_redirect)

[玩的不是存储，是上百万资产！DELL PowerStore 入门白嫖实操体验](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494769&idx=1&sn=e56fdc0c08ea4cb94e0fb99267b4d0ad&scene=21#wechat_redirect)

[突发！17000个Citrix云桌面某分支无法登录，PVS云桌面启动后的致命错误背后是...？](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494467&idx=1&sn=646638b58dc236eadec311763cc2b5a7&scene=21#wechat_redirect)

[全网独创VMware Horizon和Citrix云桌面免费对接Radius进行Google二次认证教程](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247490194&idx=1&sn=fcd70e32f27a6e0db1936d551a7e9aa7&scene=21#wechat_redirect)

[Citrix Virtual Apps and Desktops云桌面内网Storefront登录流程详解](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247486323&idx=1&sn=2f90293c8d6a57e65744a2a2eb349767&scene=21#wechat_redirect)

[零基础企业级云桌面搭建实战：VMware Horizon与AVI负载均衡详解（上）](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494647&idx=1&sn=662312138478d9c82fecd4e56baaad9c&scene=21#wechat_redirect)

[ESXi添加利旧硬盘踩雷！无法创建VMFS数据存储？我用一条命令挣了1000块](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494661&idx=1&sn=0f23ad44eb4140846440ef458134d6f2&scene=21#wechat_redirect)

[一条命令获取VMware ESXi硬件全家桶信息，你Get到了吗？](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494675&idx=1&sn=77ac576335bf0ba4f55fb920de490cec&scene=21#wechat_redirect)

[各大云平台KVM、AWS、PVE和VMware虚拟机文件格式互转换技巧](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494294&idx=1&sn=d590a88d33a8e218cbba1bd82e2976c0&scene=21#wechat_redirect)

[VMware ESXi和Workstation无法导入ovf虚拟机解决宝典|红包到手500](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494277&idx=1&sn=c0abe58439e5bc2116828204a85ca778&scene=21#wechat_redirect)

[限制特定ip访问 VMware ESXi 和 vCenter主机](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494259&idx=1&sn=2e19762846f9412ac2c2ae761cdf10c2&scene=21#wechat_redirect)

[多种方法开启VMware ESXi Shell和超时方法，你知道哪些？](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494201&idx=1&sn=39b54acbf66401a7febfd55e34ccc3b2&scene=21#wechat_redirect)

[VMware vSphere中弃用和停止的CPU有哪些？你真的了解吗？](https://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247494161&idx=1&sn=209c7bb956668d85cad71aa76bc31817&scene=21#wechat_redirect)

[VMware vCenter(VCSA) 8.0 中的 17 个vmdk有什么用途？给客户介绍用途含泪血赚200！](http://mp.weixin.qq.com/s?__biz=Mzg2NzQ0MDc5Mg==&mid=2247488683&idx=1&sn=044addd224004cf1a5501909f3f59f39&chksm=cebad55cf9cd5c4a78e68d6ddb0b0bb00c606776741ccc4d41bc442dc2003b318651fd13bd4b&scene=21#wechat_redirect)

资源申明:小编更新资源文章只是为了给大家提供一个绿色学习的平台，如果你在本站看到的任何图片文字有涉及到你的利益以及版权都可以联系小编删除。联系微信：XNHSDJ