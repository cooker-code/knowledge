# 030301_Flink知识地图

## 全景知识域

全景按 Flink 的技术问题域**自顶向下**定义，不依赖已有文章数量。先看全景再匹配已有知识点，空白域就是当前薄弱点。

| 域 | 核心问题 | 覆盖状态 |
|---|---|---|
| A 运行模型 | Job/Task/Slot/算子链、并行度、资源隔离如何影响执行 | ✓ 覆盖 |
| B 时间与窗口 | 事件时间/Watermark/窗口触发/迟到数据如何控制结果正确性 | ✓ 覆盖 |
| C 状态体系 | 状态类型/后端/KeyGroup/TTL/扩缩容如何综合决定性能和正确性 | ✓ 覆盖 |
| D 容错 | Checkpoint 链路/Savepoint/Restart Strategy 如何保障一致性 | ✓ 覆盖 |
| E 流 Join | Regular/Interval/Temporal/Lookup 的状态语义和膨胀边界 | ✓ 覆盖 |
| F SQL 抽象 | Planner/执行计划/状态算子/提交治理如何影响生产作业 | ✓ 覆盖 |
| G Connector | Source/Sink 语义、Kafka/CDC/JDBC/Paimon 接入边界 | ✓ 覆盖 |
| H 生产化 | 反压/监控指标/升级/资源调优的诊断路径 | ✓ 覆盖 |

**覆盖说明**：✓ 主要判断准则已沉淀；△ 有入口但不完整；✗ 无对应知识点。

## 已沉淀知识点

| 域 | 知识点文件 | 解决问题 | 核心结论 |
|---|---|---|---|
| B | [Flink窗口触发与状态淘汰边界](030301_核心知识点/Flink窗口触发与状态淘汰边界.md) | 窗口何时触发、迟到数据如何处理、Evictor 与业务过滤的区别 | 事件时间窗口由 Watermark + Timer 触发；Evictor 影响状态本身，不是计算时业务过滤 |
| C | [Flink状态后端选型](030301_核心知识点/Flink状态后端选型.md) | 不同状态后端如何影响延迟、Checkpoint、恢复和状态规模 | 状态后端选型是性能、可靠性、扩展性和运维成本的综合权衡，不是单参数选择 |
| C | [FlinkKeyGroup状态分配与扩缩容](030301_核心知识点/FlinkKeyGroup状态分配与扩缩容.md) | Flink 如何以 KeyGroup 为单位管理状态并支持扩缩容 | 并行度变化不是状态分片根源，`maxParallelism` 才是 KeyGroup 总数的固定锚点 |
| C | [FlinkStateTTL状态生命周期治理](030301_核心知识点/FlinkStateTTL状态生命周期治理.md) | TTL 如何影响状态可见性、物理清理、Checkpoint 和业务正确性 | TTL 是业务语义和资源治理的共同边界，不是通用的状态瘦身参数 |
| D | [FlinkCheckpoint完整链路与Savepoint边界](030301_核心知识点/FlinkCheckpoint完整链路与Savepoint边界.md) | 将 Checkpoint 拆成触发、对齐、快照、持久化、ACK、完成和恢复阶段 | Checkpoint 排障要按阶段定位；Savepoint 是计划维护和迁移边界 |
| D | [Flink通用增量Checkpoint](030301_核心知识点/Flink通用增量Checkpoint.md) | Changelog State Backend 为什么能降低 Checkpoint 完成时间，以及代价 | 增量 Checkpoint 不是免费优化，本质是用空间、网络和恢复重放成本换稳定性 |
| E | [Flink双流Join状态膨胀与构建端选择](030301_核心知识点/Flink双流Join状态膨胀与构建端选择.md) | Flink Join 状态为什么膨胀以及 Hint 的使用边界 | "谁小谁先"要校准为"Join 类型、时间边界和 Watermark 共同决定状态上限" |
| F | [FlinkSQL大状态作业调优](030301_核心知识点/FlinkSQL大状态作业调优.md) | 建立 Flink SQL 大状态作业的诊断顺序和状态算子来源判断 | 大状态调优要先看执行计划和状态语义，不能先堆资源或盲目缩短 TTL |
| F | [FlinkSQL脚本化提交与Application模式适配](030301_核心知识点/FlinkSQL脚本化提交与Application模式适配.md) | Flink SQL 如何从交互式开发走向文件化、版本化、可校验和生产提交 | "像 Hive 一样用 Flink SQL"的核心是作业治理，不是简单按分号拆 SQL |
| H | [Flink反压排查入口](030301_核心知识点/Flink反压排查入口.md) | 建立 Flink 反压的基础排查路径 | 反压是下游瓶颈向上游传播的保护机制，会同时影响 Watermark 和 Checkpoint |
| A | [Flink运行模型与资源管理](030301_核心知识点/Flink运行模型与资源管理.md) | 作业如何从用户代码变成物理任务，并行度/Slot/算子链三者关系 | 用户代码经过 StreamGraph→JobGraph→ExecutionGraph 多级转换，算子链合并减少跨 Task 通信 |
| A | [FlinkBatchProcessing批处理调度](030301_核心知识点/FlinkBatchProcessing批处理调度.md) | Flink 批处理 Hybrid/Blocking Shuffle 选择，增量批计算适用场景 | 动态并行度 + Blocking Shuffle 性能更优；Hybrid Shuffle 在资源充足时可避免全量落盘 |
| A | [FlinkNetworkStack网络堆栈](030301_核心知识点/FlinkNetworkStack网络堆栈.md) | Flink 任务间数据如何传输，基于信用的流控如何解决反压 | 基于 Credit 的流控在 Task 粒度替代 TCP 全局反压，解决 head-of-line blocking 问题 |
| A | [Flink内存模型](030301_核心知识点/Flink内存模型.md) | TaskManager 内存各区域构成，RocksDB 使用哪块内存，如何配置避免 OOM | RocksDB 使用托管内存（堆外），需通过 `taskmanager.memory.managed.fraction` 控制；框架堆/任务堆/网络内存独立配置 |
| A | [Flink部署环境与JDK版本管理](030301_核心知识点/Flink部署环境与JDK版本管理.md) | Flink 2.x 要求 JDK 11 而 Hadoop 固定 JDK 8 时如何解决环境冲突 | 用 `yarn.ship-archives` 将 JDK 包随 Application 分发到每个 Container，不改集群环境 |
| A | [Flink2.0关键变化与选型影响](030301_核心知识点/Flink2.0关键变化与选型影响.md) | Flink 2.0 哪些变化影响升级路径和架构选型 | ForSt DB 存算分离解决大状态迁移痛点；Materialized Table 统一流批代码层面但不解决语义一致性 |
| B | [Flink窗口高级特性](030301_核心知识点/Flink窗口高级特性.md) | 窗口提前触发/offset 打散/滑动窗口实战 | early-fire 让窗口持续输出中间结果；offset 打散触发时刻避免惊群效应 |
| C | [FlinkRocksDB深度调优](030301_核心知识点/FlinkRocksDB深度调优.md) | RocksDB 默认配置在 10GB+ 状态时出现性能问题的调优优先级 | MemTable 增大减少 flush；Block Cache 增大提升读命中率；Bloom Filter 默认关需手动开 |
| C | [FlinkStateBackend与CheckpointStorage解耦](030301_核心知识点/FlinkStateBackend与CheckpointStorage解耦.md) | 1.13+ StateBackend 与 CheckpointStorage 解耦后各自职责边界 | StateBackend 管运行时存储介质；CheckpointStorage 管快照持久化位置；两者可独立组合 |
| C | [FlinkBroadcastState动态配置更新](030301_核心知识点/FlinkBroadcastState动态配置更新.md) | 不停作业地动态更新规则/配置的实现模式 | Broadcast State 只能在广播侧写入；每个并行实例独立持有副本；仅支持 Map 类型且存内存 |
| C | [FlinkStateUsagePatterns状态使用陷阱](030301_核心知识点/FlinkStateUsagePatterns状态使用陷阱.md) | 有状态生产作业的高频故障：状态膨胀、Checkpoint 超时、乱序 merge 错误 | 状态故障 80% 来自未设 TTL/Key 倾斜/窗口未清理三类根因 |
| C | [FlinkState存算分离探索](030301_核心知识点/FlinkState存算分离探索.md) | RocksDB 大状态结构性痛点，阿里 Gemini 和 B 站 Remote State 的解法 | 存算分离的核心价值是解耦本地磁盘依赖，使扩缩容和故障恢复不需全量重放本地 I/O |
| C | [Flink大状态优化实战](030301_核心知识点/Flink大状态优化实战.md) | 100GB+ 状态作业默认配置失效时的排查顺序和实测数据 | 先确认 CP 耗时和 OOM 症状，按增量 CP→RocksDB 参数→并行度→TTL 顺序逐项排查 |
| D | [Flink重启异常与状态丢失排障](030301_核心知识点/Flink重启异常与状态丢失排障.md) | 作业重启失败是状态丢失还是配置错误，stop-with-savepoint 可靠性 | CP 失败根因优先级：反压 > 存储 I/O > JVM GC > 网络超时；stop-with-savepoint 中断会丢失状态 |
| D | [StateProcessorAPI离线状态读写](030301_核心知识点/StateProcessorAPI离线状态读写.md) | 如何离线读取/修改 Savepoint 中的状态数据 | State Processor API 将 Savepoint 公开为 DataSet，可修复状态错误、导入初始化数据、调整算子 UID |
| E | [FlinkJoin技术全景](030301_核心知识点/FlinkJoin技术全景.md) | 五种 Join 类型在状态、时间依赖、结果语义上的全面对比 | Regular Join 状态无界需配 TTL；Interval Join 有时间边界自动清理；Lookup Join 无状态查外部 |
| E | [FlinkLookupJoin维表关联实践](030301_核心知识点/FlinkLookupJoin维表关联实践.md) | 五种维表关联方案在数据量/更新延迟/吞吐/实现难度上的选型 | 异步 Lookup + 本地缓存是高吞吐首选；广播内存放不下时用热存储异步；预加载适合小静态维表 |
| F | [FlinkSQLPlanner与执行计划](030301_核心知识点/FlinkSQLPlanner与执行计划.md) | Old Planner 与 Blink Planner 区别，SQL 如何变成底层算子 | 1.14+ 只保留 Blink Planner；SQL→RelNode→优化规则→StreamGraph 路径是调试执行计划的关键 |
| F | [FlinkSQLChangelog回撤机制](030301_核心知识点/FlinkSQLChangelog回撤机制.md) | 聚合结果出现负数、-U 消息、Deduplication 回撤的根本原因 | 流上聚合结果变化时必须先发 -U 撤回旧值再发 +U 更新，下游需能处理回撤否则数据错误 |
| F | [FlinkSQL开发陷阱](030301_核心知识点/FlinkSQL开发陷阱.md) | Flink SQL 看似正确但结果错误或作业崩溃的隐蔽陷阱系统梳理 | 空闲分区阻塞 Watermark、事件时间/处理时间混用、强类型转换、View 多消费状态膨胀是四大高频陷阱 |
| F | [FlinkSQL流批统一与混合编程](030301_核心知识点/FlinkSQL流批统一与混合编程.md) | Flink SQL 和 DataStream API 何时选哪个，流批模式如何切换 | 切换 `execution.runtime-mode` 实现同一 SQL 流批复用；SQL 限制场景用混合 API 嵌入 DataStream 逻辑 |
| F | [FlinkSQL字段血缘](030301_核心知识点/FlinkSQL字段血缘.md) | 从 Flink SQL INSERT 语句自动提取字段级血缘关系 | 基于 CalciteParser + RelMetadataQuery 三步提取法；Lookup Join 血缘需额外适配 RelColumnOrigin |
| F | [FlinkUDF自定义函数](030301_核心知识点/FlinkUDF自定义函数.md) | 三类 UDF 的继承方式、方法约定和生产监控/缓存注意事项 | ScalarFunction/TableFunction/AggregateFunction 各有固定约定方法；生产建议加 MetricGroup 监控和结果缓存 |
| F | [Flink SQL优化准则](030301_核心知识点/Flink SQL优化准则.md) | Flink SQL 性能不足时哪些优化手段在 2025 年仍有效 | Mini-Batch + Lookup Cache + TopN 优化推荐指数最高；算子级 TTL（1.18+）解决细粒度状态保留 |
| G | [FlinkConnectorFormat序列化](030301_核心知识点/FlinkConnectorFormat序列化.md) | JSON/Avro/Protobuf 三种格式在性能、Schema 演进上的核心差异 | 高吞吐选 Protobuf/Avro（二进制）；需要 Schema 演进选 Avro + Schema Registry；快速开发选 JSON |
| G | [FlinkDorisConnector写入机制](030301_核心知识点/FlinkDorisConnector写入机制.md) | Flink 写 Doris 如何保证高吞吐与 Exactly-Once，batch-sink 与 CP 模式区别 | Exactly-Once 依赖 Checkpoint 触发 2PC Commit；batch-sink 双队列独立刷写可达更高吞吐但只有 At-Least-Once |
| G | [FlinkHiveCatalog集成](030301_核心知识点/FlinkHiveCatalog集成.md) | Flink SQL 元数据如何持久化跨 Session 共享，HiveCatalog 与 HiveConnector 关系 | HiveCatalog 负责元数据持久化（HMS），HiveConnector 负责读写 Hive 表数据；两者独立可分开使用 |
| G | [FlinkJDBCConnector内部实现](030301_核心知识点/FlinkJDBCConnector内部实现.md) | JdbcInputFormat 如何跨三层 Graph 传递，并发读取 MySQL 如何切分任务 | JdbcInputFormat 在 JobGraph 层序列化为二进制存入 JobVertex 配置；按 `numPartitions` 参数分片并发读 |
| H | [FlinkWebUI指标解读](030301_核心知识点/FlinkWebUI指标解读.md) | Web UI 各指标含义，拿到有问题的作业应按什么顺序看哪些指标 | 先看 Checkpoints（是否超时）→ 看 Backpressure（高亮算子）→ 看 Metrics（内存/GC）三步诊断法 |
| H | [Flink监控告警体系](030301_核心知识点/Flink监控告警体系.md) | 大规模生产作业应持续监控哪些指标，如何建立有效告警体系 | 应用级自定义指标 + Prometheus 内置指标 + 系统级 K8s 指标三层组合；Web UI 不能替代外部监控 |
| H | [Flink调优方法论](030301_核心知识点/Flink调优方法论.md) | 面对性能问题的 Flink 作业应按什么顺序排查，各项优化如何操作 | 调优顺序：反压排查 → 并行度 → 内存/序列化 → Checkpoint → 算子优化；不能跳步骤 |
| H | [Flink生产最佳实践](030301_核心知识点/Flink生产最佳实践.md) | 生产 Flink 作业从开发到运维的关键准则 | 开发强制项：设算子 uid / TTL / 异常 catch；部署强制项：CP 开 Exactly-Once / 设最大并行度锚点 |
| H | [FlinkHA与JobManager高可用](030301_核心知识点/FlinkHA与JobManager高可用.md) | JobManager 单点故障如何通过 HA 解决，ZK HA 和 K8s HA 如何选择 | HA 核心是 Active/Standby 选主 + 元数据持久化；K8s HA（1.12+）无需额外 ZK 集群，云原生首选 |
| H | [实时流脏数据治理](030301_核心知识点/实时流脏数据治理.md) | 实时流中如何在数据流入时治理脏数据，如何设计可追溯的清洗架构 | 四步治理：分类识别 → 侧输出隔离 → 旁路存储 → 补偿重放；"事后清洗"在实时场景因无状态无法回溯 |
| H | [Flink实时特征与变量计算架构模式](030301_核心知识点/Flink实时特征与变量计算架构模式.md) | 金融风控/推荐场景如何设计 Flink 架构满足历史回溯、快速迭代、多流关联 | 风控/金融场景推荐 Kappa 架构保证一致性；历史回溯用 Savepoint 初始化状态而非离线补数 |
| H | [FlinkCEP复杂事件处理](030301_核心知识点/FlinkCEP复杂事件处理.md) | CEP 底层 NFA 状态管理、连续性策略性能差异、动态规则更新 | followedByAny 最宽松但状态最大；通过 Broadcast State 下发规则实现不停作业的动态 CEP 更新 |

## 待补缺口

| 域 | 具体缺口 | 优先级 |
|---|---|---|
| B 时间与窗口 | 缺固定输入序列实验：Watermark/侧输出/Evictor 联动边界未通过实测验证 | 中 |
| C 状态体系 | TTL 与业务正确性实测验证（边界 case 缺测试数据） | 中 |
| D 容错 | Checkpoint 阶段化指标（对齐/快照/上传/ACK/恢复时间）实测验证 | 中 |
| F SQL 抽象 | SQL Gateway 生产链路、Multi-Statement 实测验证 | 中 |
| G Connector | Kafka/CDC/Paimon Sink Exactly-Once 和 Schema 演进实测验证 | 中 |
