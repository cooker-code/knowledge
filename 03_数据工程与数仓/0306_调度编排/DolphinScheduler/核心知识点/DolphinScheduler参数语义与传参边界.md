# DolphinScheduler 参数语义与传参边界

## 原文锚点

- 本地文件：[实战干货：Apache DolphinScheduler 参数使用与优化总结](../文章/实战干货：Apache DolphinScheduler 参数使用与优化总结.md)
- 原文链接：https://mp.weixin.qq.com/s?__biz=MzkwNTg3MzY3Mg==&mid=2247524132&idx=1&sn=bbca9457b018859101b79c283d351ce9
- 关键段落：内置参数、衍生函数、工作流全局参数、SQL/SHELL/PYTHON 跨任务传参、参数优先级。
- 关键图：原文多处提到配置截图，但本地 Markdown 无图。

## 图片处理

| 图片 | 类型 | 是否保留 | 理由 | 处理方式 |
|---|---|---|---|---|
| 参数配置截图 | 说明图 | 原图缺失 | 展示 UI 配置位置，对实践复现有帮助 | 标记原图缺失，需要回原文查看 |

## 一句话结论

这篇文章适合精读，它补的是 DolphinScheduler 参数体系的作用域、传递方向和优先级，不是 DolphinScheduler 调度架构全貌。

## 用户相关性判断

| 项 | 内容 |
|---|---|
| 用户当前认知层级 | 调度编排 L2 draft |
| 认知成熟度 | draft |
| 阅读投入建议 | 精读 |
| 阅读投入理由 | 参数规则对调度任务复用和补数很关键，但缺完整可复现环境和截图 |
| 对用户的新信息 | 参数应按时间基准、作用域、传递方向和优先级理解，而不是普通字符串替换 |
| 问题指纹 | DolphinScheduler + 参数体系 + 内置参数/衍生函数/全局参数/上下游传参 + 任务复用 + 版本边界 |
| 排重判断 | 新建 |
| 置信度 | 高 |

## 认知校准点

| 校准点 | 文章观点/信息 | 与用户认知或价值观的关系 | 处理建议 |
|---|---|---|---|
| 参数是调度语义 | `system.biz.date`、`system.biz.curdate` 等绑定调度实例时间 | 补充：调度参数不只是模板变量 | 写入 DolphinScheduler index |
| SQL 场景要关注衍生函数 | 原文建议 SQL 下优先使用 `$[...]` 衍生函数 | 补充：不同节点类型参数语法不同 | 后续实践验证版本差异 |
| 传参有方向和类型 | IN 只当前节点，OUT 可向下游传递；SQL/SHELL 可输出，SQL/SHELL/PYTHON 可接收 | 纠偏：不是所有节点都能任意传参 | 记住传参边界 |
| 参数优先级会改变结果 | 本地参数 > 上游传递参数 > 全局参数 | 补充：这是排查参数错乱的第一准则 | 写入排障清单 |

## 冲突点

| 冲突类型 | 具体表现 | 影响 | 处理 |
|---|---|---|---|
| 图片缺失 | 多处“如下图”但本地无图 | 影响 UI 操作复现 | 标记原图缺失 |
| 实践判定偏宽 | 有示例但缺完整工作流导出、数据和环境 | 不能直接判实践 | 降为精读 |
| 版本边界 | 原文基于 DolphinScheduler 3.1.9 | 后续版本参数行为可能变化 | 实践前查官方文档 |
| 原文局限 | 只讲参数，不讲 Master/Worker/调度实例状态 | 不能当 DolphinScheduler 全貌 | 只沉淀为参数主题 |

## 待吸收点

| 分级 | 内容 | 为什么值得吸收 | 后续动作 |
|---|---|---|---|
| 理解 | 内置参数表示调度实例的业务日期、当前日期和时间戳 | 数仓跑批常依赖业务日期 | 与补数场景关联 |
| 理解 | `$[...]` 衍生函数适合日期格式拼接和加减 | 解决 SQL 节点日期参数可读性 | 后续验证常见写法 |
| 记住 | IN/OUT 是传参边界，OUT 才能向下游传递 | 防止跨节点参数误用 | 写入 DolphinScheduler index |
| 记住 | 本地参数优先级最高，其次上游传参，最后全局参数 | 参数错乱排障第一步 | 后续做检查清单 |
| 实践 | 用 Shell -> SQL -> Python 三节点验证传参 | 可以形成可复现最小例子 | 待实验 |

## 已知可跳过

| 内容 | 跳过理由 |
|---|---|
| 参数能提高任务复用性 | 基础常识 |
| 客户案例和社区宣传 | 不影响参数理解 |
| 发版消息列表 | 资讯信息，不进入核心知识点 |

## 实践门槛

| 门槛 | 判断 | 证据 |
|---|---|---|
| 可运行 | 部分 | 有 SQL/SHELL/PYTHON 片段 |
| 可验证 | 部分 | 有运行结果描述，但截图缺失 |
| 可排障 | 部分 | 有参数优先级和 bug 提醒，但缺日志定位 |
| 可迁移 | 是 | 可迁移到数仓调度任务 |
| 结论 | 降为精读 | 缺完整环境和导出工作流 |

## 归类判断

| 项 | 内容 |
|---|---|
| 技术本体 | DolphinScheduler 是数据工作流调度平台 |
| 文章主问题 | DolphinScheduler 参数如何声明、计算、传递和覆盖 |
| 使用场景 | 跑批日期、工作流全局变量、上游查询结果传给下游任务 |
| 关键词干扰 | 实战、SQL、Python、客户案例 |
| 最终归类 | 数据工程与数仓 / 调度编排 / DolphinScheduler |
| 归类理由 | 主问题是调度平台参数体系，不是 SQL 引擎或 Python 编程 |

## 纵向理解

| 维度 | 判断 |
|---|---|
| 全局架构 | 参数体系位于工作流定义、任务实例运行和节点传参之间 |
| 本文位置 | 只讲参数使用，不讲调度器架构 |
| 核心机制 | 内置时间参数、衍生日期函数、全局参数、IN/OUT、本地优先级 |
| 使用链路 | 定义工作流参数 -> 节点引用 -> 上游输出 -> 下游接收 -> 按优先级解析 |
| 前置条件 | 明确节点类型、DolphinScheduler 版本、业务日期语义 |
| 边界 | 参数不能替代数据质量校验和补数策略 |

## 横向对标

| 对标技术 | 实现方式 | 优势 | 劣势 | 适合场景 |
|---|---|---|---|---|
| DolphinScheduler 参数 | UI/任务节点内置和传参 | 数据任务可视化管理友好 | 语法受节点类型约束 | 平台化数仓调度 |
| Airflow Variables/XCom | Python DAG 中变量和任务间传值 | 可编程灵活 | 对非 Python 用户门槛更高 | 代码化调度和复杂逻辑 |
| Shell 环境变量 | 脚本运行时注入 | 简单通用 | 缺调度实例语义和依赖关系 | 简单脚本 |
| Hive/Spark SQL 参数 | SQL 引擎变量替换 | 靠近执行引擎 | 不负责 DAG 传参 | 单任务 SQL |

## 后续追查

- 关键词：DolphinScheduler parameter、global parameter、local parameter、OUT、system.biz.date。
- 相关技术：Airflow XCom、Azkaban flow parameters、Hive/Spark SQL 参数。
- 需要补读的文章：DolphinScheduler 补数、任务失败策略、资源队列、参数官方文档。
